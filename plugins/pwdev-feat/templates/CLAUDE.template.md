# CLAUDE.md — Project Conventions

> Generated by `/pwdev-feat:setup`

---

## 1. Project Identity

**Name:** [project name]
**Description:** [one sentence]
**Type:** [web app / API / CLI / library / mobile]

---

## 2. Stack

```
Language:     [to be filled]
Framework:    [to be filled]
Database:     [to be filled]
Tests:        [to be filled]
E2E:          Playwright
Lint:         [to be filled]
```

---

## 3. Architecture

**Pattern:** [MVC / Clean Architecture / DDD / Hexagonal / other]
**API style:** [REST / GraphQL / gRPC]
**Auth:** [JWT / Session / OAuth / other]

---

## 4. Conventions

### Naming
- **Files:** [kebab-case / PascalCase / snake_case]
- **Functions:** [camelCase / snake_case]
- **Components:** [PascalCase]
- **Database:** [snake_case / plural tables]

### Git
- Commit style: Conventional Commits (`feat`, `fix`, `refactor`, `test`, `docs`, `chore`)
- Branch: `feature/{description}`, `fix/{description}`

### Code
- [project-specific conventions detected or defined]

---

## 5. Commands

```bash
# Development
[dev command]

# Lint
[lint command]

# Tests
[test command]

# E2E
npx playwright test

# Build
[build command]
```

---

## 6. Quality Rules

- Tests required for all business logic
- Playwright E2E for all UI features
- Lint must pass before commit
- TypeScript strict mode (if applicable)
- No `any` without justification

---

## 7. Security

```
NEVER store, log, or display secrets
NEVER commit .env or credentials
NEVER hardcode API URLs
NEVER execute destructive actions without confirmation
NEVER use deprecated libraries with known CVEs
```

---

## 8. Golden Rules

```
1. Read CLAUDE.md before starting any task.
2. Follow the stack and conventions defined here.
3. One task, one commit, one scope.
4. Tests are not optional.
5. Ask when in doubt — don't assume.
```
