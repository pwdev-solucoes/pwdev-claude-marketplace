---
name: schema-markup
description: >
  Implementa e valida dados estruturados schema.org em JSON-LD — Organization, Article, FAQPage, Product, BreadcrumbList. Use quando o usuário disser "schema", "dados estruturados", "JSON-LD", "rich snippet", "FAQ schema", "schema.org".
metadata:
  version: 0.1.0
  status: stub — estrutura definida, conteúdo a preencher
  derivado-de: schema-markup (catálogo original, portado)
---

# schema-markup

> **STUB.** A anatomia abaixo é a DNA compartilhada por todas as skills do
> pwdev-copy. Preencher as seções marcadas com `TODO`.

## Papel
Especialista em dados estruturados.

## Antes de escrever
Leia `.claude/pwdev-copy-context.md`. Pergunte apenas o que não estiver lá.

Portão: se a **seção 3** (posicionamento, promessa, big idea) estiver vazia,
pare e rode `/pwdev-copy:brief`.

TODO — perguntas específicas deste formato.

## Princípio central
Schema precisa refletir o que está visível na página. Marcar o que não existe na tela é violação de diretriz e gera penalidade.

## Framework
Identificar tipos aplicáveis → gerar JSON-LD → validar (Rich Results Test, Schema Validator) → implantar por stack → checklist de teste.

TODO — detalhar cada etapa.

## Regras inegociáveis
1. Jamais inventar número, depoimento ou certificação — usar `[PREENCHER: ...]`.
2. Respeitar a lista de proibidos (seção 5) e os vetos jurídicos (seção 7).
3. Usar o vocabulário literal do VOC (seção 6), não a tradução corporativa.

## Formato de saída
TODO — seções fixas + alternativas com ângulos distintos + lacunas consolidadas.

## Anti-padrões
TODO.

## Skills relacionadas
- `seo-audit` — contexto
- `copy-page` — FAQ da página alimenta o FAQPage
