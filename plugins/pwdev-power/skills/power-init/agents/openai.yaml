interface:
  display_name: "PWDEV Power Init"
  short_description: "Initialize the workspace and map the codebase"
  default_prompt: "Use $power-init to initialize the workspace or refresh the codebase map."
