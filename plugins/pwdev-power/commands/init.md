---
description: Initialize or resume the PWDEV Power workspace, map the codebase, and check its runtime surface
argument-hint: "[--map | --check]"
---

# /pwdev-power:init

Read `${CLAUDE_PLUGIN_ROOT}/skills/power-init/SKILL.md` completely, including every directly required reference. Execute that skill as the active Claude Code adapter.

User arguments: $ARGUMENTS
