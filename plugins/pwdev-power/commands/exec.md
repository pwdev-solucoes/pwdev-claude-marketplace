---
description: Execute an approved plan task by task, with a durable ledger and review between tasks
argument-hint: "<feature-slug>"
---

# /pwdev-power:exec

Read `${CLAUDE_PLUGIN_ROOT}/skills/power-execute/SKILL.md` completely, including every directly required reference. Execute that skill as the active Claude Code adapter. Dispatch the packaged implementer and task-reviewer subagents; never implement in this context.

User arguments: $ARGUMENTS
