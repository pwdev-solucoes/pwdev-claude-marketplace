---
description: Adversarially verify an implementation and decide how to integrate it
argument-hint: "<feature-slug> [--strict]"
---

# /pwdev-power:verify

Read `${CLAUDE_PLUGIN_ROOT}/skills/power-verify/SKILL.md` completely, including every directly required reference. Execute that skill as the active Claude Code adapter. Try to refute completion with real commands before accepting it.

User arguments: $ARGUMENTS
