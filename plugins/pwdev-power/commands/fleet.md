---
description: Launch, inspect, or tear down an isolated autonomous phase fleet in cmux
argument-hint: "<slug...> [--via-kanban] | --status | --teardown <slug> [--merge]"
disable-model-invocation: true
---

# /pwdev-power:fleet

Read `${CLAUDE_PLUGIN_ROOT}/skills/power-fleet/SKILL.md` completely, including every directly required reference. Execute that skill as the active Claude Code adapter: launch through the packaged `claude-fleet-up.sh`, disclose and require acknowledgement of the native `claude -p` vector, and never another runtime one. Do not accept a runtime override from arguments or configuration.

User arguments: $ARGUMENTS
