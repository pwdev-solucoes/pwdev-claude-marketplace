---
description: Launch a visual cmux panel of 1-4 phase sessions, or an unattended fleet, and inspect or tear it down
argument-hint: "<slug...> [--auto] [--via-kanban] | --status | --teardown <slug> [--merge]"
disable-model-invocation: true
---

# /pwdev-power:fleet

Read `${CLAUDE_PLUGIN_ROOT}/skills/power-fleet/SKILL.md` completely, including every directly required reference. Execute that skill as the active Claude Code adapter: launch through the packaged `claude-fleet-panel.sh` by default or `claude-fleet-up.sh` under `--auto`, disclose and require acknowledgement of the native Claude vector for the mode you are launching, and never another runtime one. Do not accept a runtime override from arguments or configuration.

User arguments: $ARGUMENTS
