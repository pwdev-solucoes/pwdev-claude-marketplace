---
description: Assess release readiness without approving or performing a release.
argument-hint: <target, build, contract, results, defects, risks, decisions, and authorization>
---

Load `${CLAUDE_PLUGIN_ROOT}/skills/qa-release/SKILL.md` and follow it exactly.
Treat `$ARGUMENTS` as the user's inputs together with the current repository context.
Preserve objective, constraints, limitations, and authorization, then return the shared skill's result unchanged.
