---
description: Initialize or inspect project-local PWDEV QA context
argument-hint: "[project-root] [target and contract context]"
---

# /pwdev-qa:init

Read `${CLAUDE_PLUGIN_ROOT}/skills/qa-init/SKILL.md`, pass through `$ARGUMENTS`
and the current repository context, and return the shared skill's result unchanged.
