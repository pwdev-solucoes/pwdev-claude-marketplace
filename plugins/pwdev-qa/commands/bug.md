---
description: Record or reassess a traceable defect and retest state.
argument-hint: <target, contract, reproduction, evidence, triage, history, and authorization>
---

Load `${CLAUDE_PLUGIN_ROOT}/skills/qa-bug/SKILL.md` and follow it exactly.
Treat `$ARGUMENTS` as the user's inputs together with the current repository context.
Preserve objective, constraints, and authorization, then return the shared skill's result unchanged.
