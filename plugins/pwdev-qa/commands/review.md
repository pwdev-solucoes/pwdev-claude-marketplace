---
description: Review requirements, coverage, evidence, and findings without mutation.
argument-hint: <target, contract, criteria, coverage, evidence, defects, and authorization>
---

Load `${CLAUDE_PLUGIN_ROOT}/skills/qa-review/SKILL.md` and follow it exactly.
Treat `$ARGUMENTS` as the user's inputs together with the current repository context.
Preserve objective, constraints, limitations, and authorization, then return the shared skill's result unchanged.
