---
description: Summarize the recorded QA state without executing or mutating anything.
argument-hint: <target, contract, recorded criteria, cases, evidence, defects, and authorization>
---

Load `${CLAUDE_PLUGIN_ROOT}/skills/qa-status/SKILL.md` and follow it exactly.
Treat `$ARGUMENTS` as the user's inputs together with the current repository context.
Preserve objective, constraints, limitations, and authorization, then return the shared skill's result unchanged.
