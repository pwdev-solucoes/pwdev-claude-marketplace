---
description: Execute selected authorized PWDEV QA tests with traceable evidence
argument-hint: "[target, contract, case selection, and authorization]"
---

# /pwdev-qa:test

Read `${CLAUDE_PLUGIN_ROOT}/skills/qa-test/SKILL.md`, pass through `$ARGUMENTS`
and the current repository context, and return the shared skill's result unchanged.
