---
description: Build a traceable impact-based regression selection.
argument-hint: <target, change, contract, criteria, risks, tests, and authorization>
---

Load `${CLAUDE_PLUGIN_ROOT}/skills/qa-regression/SKILL.md` and follow it exactly.
Treat `$ARGUMENTS` as the user's inputs together with the current repository context.
Preserve objective, constraints, and authorization, then return the shared skill's result unchanged.
