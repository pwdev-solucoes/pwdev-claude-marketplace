---
description: Delegate a bounded repository task to a guarded external coding CLI
argument-hint: "<provider> <read|write> <task>"
disable-model-invocation: true
---

# /pwdev-flow:delegate

Read `${CLAUDE_PLUGIN_ROOT}/skills/flow-delegate/SKILL.md` completely, including every directly required reference. Execute that skill as the active Claude Code adapter and keep standalone delegation separate from both fleet bypass vectors and require the complete expanded command confirmation.

User arguments: $ARGUMENTS
