---
description: Inventory, archive, or summarize PWDEV Flow artifacts safely
argument-hint: "<inventory|archive|summarize> [arguments]"
---

# /pwdev-flow:maintenance

Read `${CLAUDE_PLUGIN_ROOT}/skills/flow-maintenance/SKILL.md` completely, including every directly required reference. Execute that skill as the active Claude Code adapter and preserve its read-only and reversible-operation gates.

User arguments: $ARGUMENTS
