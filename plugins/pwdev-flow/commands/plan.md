---
description: Decompose an approved PWDEV Flow specification into executable tasks
argument-hint: "<feature-slug>"
---

# /pwdev-flow:plan

Read `${CLAUDE_PLUGIN_ROOT}/skills/flow-plan/SKILL.md` completely, including every directly required reference. Execute that skill as the active Claude Code adapter and produce a test-first plan with explicit gates.

User arguments: $ARGUMENTS
