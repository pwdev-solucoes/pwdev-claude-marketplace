---
description: Launch, inspect, or tear down an isolated native autonomous phase fleet
argument-hint: "<slug...>|--status|--teardown <slug> [--merge]"
disable-model-invocation: true
---

# /pwdev-flow:fleet

Read `${CLAUDE_PLUGIN_ROOT}/skills/flow-fleet/SKILL.md` completely, including every directly required reference. Execute that skill as the active Claude Code adapter: launch through the packaged `claude-fleet-up.sh`, disclose and require acknowledgement of the native `claude -p` vector, and never the Codex one. Do not accept a runtime override from arguments or configuration.

User arguments: $ARGUMENTS
