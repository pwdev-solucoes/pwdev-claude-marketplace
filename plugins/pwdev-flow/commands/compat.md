---
description: Inspect or plan migration from legacy PWDEV Code artifacts
argument-hint: "<inspect|plan|migrate> [arguments]"
---

# /pwdev-flow:compat

Read `${CLAUDE_PLUGIN_ROOT}/skills/flow-compat/SKILL.md` completely, including every directly required reference. Execute that skill as the active Claude Code adapter and preserve source artifacts and migration approval gates.

User arguments: $ARGUMENTS
