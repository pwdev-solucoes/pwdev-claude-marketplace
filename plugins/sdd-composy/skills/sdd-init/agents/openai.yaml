interface:
  display_name: "SDD Init"
  short_description: "Initialize or resume an SDD Composy workspace"
  default_prompt: "Use $sdd-init to inspect, plan, apply, and verify the shared SDD Composy workspace."
