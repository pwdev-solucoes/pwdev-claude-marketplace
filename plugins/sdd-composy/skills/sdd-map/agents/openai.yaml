interface:
  display_name: "SDD Map"
  short_description: "Read-only evidence map of the repository"
  default_prompt: "Use $sdd-map to scan the repository and, on request, publish the context bundle."
