---
description: Create or revise an SDD Composy technical specification
argument-hint: "<prd-slug>"
---

# /sdd-composy:techspec

Route this request exclusively to the portable `$sdd-techspec` skill. Read
`${CLAUDE_PLUGIN_ROOT}/skills/sdd-techspec/SKILL.md`, pass through `$ARGUMENTS` and the
current repository context, and return the shared skill's result unchanged.
