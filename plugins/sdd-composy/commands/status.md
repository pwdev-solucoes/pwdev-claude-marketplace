---
description: Inspect the consolidated SDD Composy status
argument-hint: "[--feature <id>] [--tasks] [--fleet] [--json]"
---

# /sdd-composy:status

Route this request exclusively to the portable `$sdd-status` skill. Read
`${CLAUDE_PLUGIN_ROOT}/skills/sdd-status/SKILL.md`, pass through `$ARGUMENTS` and the
current repository context, and return the shared skill's result unchanged.
