---
description: Record, inspect, build, query, or verify the SDD Composy semantic trace
argument-hint: "<events|summary|verify|build|query|verify-projection> [arguments]"
---

# /sdd-composy:trace

Route this request exclusively to the portable `$sdd-trace` skill. Read
`${CLAUDE_PLUGIN_ROOT}/skills/sdd-trace/SKILL.md`, pass through `$ARGUMENTS` and the
current repository context, and return the shared skill's result unchanged.
