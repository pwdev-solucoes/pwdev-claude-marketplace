---
description: Create or revise an SDD Composy product requirements document
argument-hint: "<product-problem-or-prd-slug>"
---

# /sdd-composy:prd

Route this request to the portable `$sdd-prd` skill. Read
`${CLAUDE_PLUGIN_ROOT}/skills/sdd-prd/SKILL.md`, pass through `$ARGUMENTS` and
the current repository context, and return the shared skill's result unchanged.
