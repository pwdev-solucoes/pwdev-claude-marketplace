---
description: Create or revise SDD Composy user stories
argument-hint: "<prd-slug>"
---

# /sdd-composy:stories

Route this request exclusively to the portable `$sdd-stories` skill. Read
`${CLAUDE_PLUGIN_ROOT}/skills/sdd-stories/SKILL.md`, pass through `$ARGUMENTS` and the
current repository context, and return the shared skill's result unchanged.
