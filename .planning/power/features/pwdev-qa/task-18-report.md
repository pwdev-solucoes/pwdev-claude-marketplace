# Task F04-18 — implementation report

Date: 2026-09-12
Branch: `codex/pwdev-qa`
Scope: workflows `qa-test` and `qa-explore`, their Claude wrappers, and focused workflow tests.

## RED

Command:

```text
python3 -m unittest tests.test_qa_workflows
```

Observed before implementation: 13 tests ran with 6 failures. Every failure was the expected
missing-behavior failure: `qa-test/SKILL.md`, `qa-explore/SKILL.md`, `commands/test.md`, and
`commands/explore.md` did not exist. The pre-existing `qa-init` and `qa-strategy` checks passed.

## GREEN

Focused command:

```text
python3 -m unittest tests.test_qa_workflows
```

Observed after implementation: 13 tests ran, all passed.

Packaged-Python regression command:

```text
/Users/paulosoares/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m unittest discover -s tests -p 'test_qa_*.py'
```

Observed: 155 tests ran in 10.785 seconds, all passed.

Additional checks:

```text
/Users/paulosoares/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m py_compile tests/test_qa_workflows.py
git diff --check
```

Observed: both completed successfully with no output. Generated bytecode caches were removed and
are not delivery artifacts.

## Implemented contract

- `qa-test` preserves objective, contract, criteria, case selection, and authorization; records
  case/criterion IDs, expected and observed values, evidence, limitations, defects, and verdict.
- `qa-explore` produces a bounded charter, chronological notes, findings, and follow-up while
  refusing to treat exploration alone as evidence of `PASS`.
- Both workflows enforce explicit authorization for load, penetration, production, and external
  effects; do not install, publish, change personal configuration, or correct product code
  without a separate request.
- Shared skill bodies contain no Claude-only runtime tokens. Claude command wrappers only load
  the shared skill, forward arguments and repository context, and return its result unchanged.

## Limitations

- Verification is deterministic contract testing of the Markdown workflows and wrappers. Real
  Claude Code, Codex, and Hermes smoke remains a later F05 responsibility and is not claimed.
- The repository-wide suite was not rerun for this bounded task. The approved ledger records
  seven unrelated pre-existing baseline failures; the complete `test_qa_*` regression is green.
