# Task F04-20 — implementation report

Date: 2026-09-13
Branch: `codex/pwdev-qa`

## Scope

Implemented the portable `qa-review` and `qa-release` workflow bodies, their thin Claude command
adapters, and focused behavioral coverage in `tests/test_qa_workflows.py`. Review is strictly
read-only. Release produces a QA opinion and recommendation but never performs or approves a
release. No product correction, installation, publication, push, merge, production action,
external effect, or personal configuration change was performed.

## RED

Command (packaged Python 3.12):

```text
/Users/paulosoares/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m unittest tests.test_qa_workflows
```

Observed result before implementation: `Ran 26 tests ... FAILED (failures=10)`. Every new
failure reported an absent `qa-review`/`qa-release` skill or command file. Existing workflow
tests remained green; the failures were intended behavioral assertions rather than import,
fixture-path, or environment errors.

## GREEN

Focused workflow suite:

```text
/Users/paulosoares/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m unittest tests.test_qa_workflows
Ran 26 tests in 0.008s
OK
```

All PWDEV QA tests:

```text
/Users/paulosoares/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m unittest discover -s tests -p 'test_qa_*.py'
Ran 168 tests in 10.225s
OK
```

Compilation and diff validation:

```text
/Users/paulosoares/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m compileall -q plugins/pwdev-qa/scripts tests/test_qa_workflows.py
exit 0

git diff --check
exit 0
```

Generated bytecode caches were removed after compilation and are not part of the change.

## Implemented contract

- `qa-review` preserves objective, target, requirements, results, limitations, findings,
  approvals, and authorization; it reviews requirements and coverage and emits traceable findings
  without correcting or mutating product, tests, contracts, evidence, approvals, or QA state.
- Review selects installed applicable specialists, beginning with
  `qa-specialist-requirements`; unavailable applicable expertise is an explicit limitation and
  specialist guidance never expands authorization.
- `qa-release` lists failures and pending items separately, consumes
  `qa-specialist-readiness`, preserves risk/limitation records, and keeps the human release/risk
  decision distinct from the QA verdict and recommendation.
- Release verdict precedence is explicit: a proven current in-scope failure is `FAIL`; otherwise
  pending information/work/evidence/risk/decision or zero applicable criteria is `BLOCKED`;
  `PASS` requires a complete catalog, every applicable criterion `PASS`, no current in-scope
  defects, and no failure or pending item.
- Both workflows preserve the production/external-effect authorization boundary, treat report
  export and stored commands as inert, and never publish, push, merge, deploy, install, or correct
  product code automatically.
- Both Claude wrappers only load the shared portable body, forward `$ARGUMENTS` with repository
  context, preserve objective/constraints/limitations/authorization, and return the shared result
  unchanged.

## Limitations

- The tests validate deterministic Markdown contracts and thin adapters; they are not the real
  Claude Code, Codex, or Hermes model smoke required later by F05.
- The repository-wide non-QA baseline was not rerun. The approved task requires focused plus all
  `test_qa_*` verification, and the ledger separately records seven unrelated pre-existing
  full-suite failures.
